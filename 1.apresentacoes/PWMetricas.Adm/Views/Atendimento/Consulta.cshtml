


<div class="app-content-header">
	<div class="container-fluid">
		<div class="row">
			<div class="col-sm-6"><h3 class="mb-0">Meus Atendimentos/Vendas</h3></div>
			<div class="col-sm-6">
				<div class="float-sm-end">
					<a asp-action="NovoAtendimento" class="custom-button ms-auto" type="button" onclick="location.href='atendimento.html'">
						NOVO ATENDIMENTO
						<img src="/icones/atendimento.png" alt="Ícone">
					</a>
				
				</div>
			</div>
		</div>
	</div>

</div>
<div class="app-content">
	<div class="container-fluid">

@* 		<div class="row">
			<div class="col-sm-12">
				<!-- Listagem de Perfis -->
				<div id="perfil-listagem">
					<table class="table table-striped">
						<thead>
							<tr>
								<th>Nome</th>
								<th>Ativo</th>
								<th>Ações</th>
							</tr>
						</thead>
						<tbody>
							@foreach (var perfil in Model.Dados)
							{
								<tr>
									<td>@perfil.Nome</td>
									<td>@(perfil.Ativo ? "Sim" : "Não")</td>
									<td>
										<a asp-action="Editar" asp-route-id="@perfil.Id" class="btn btn-primary btn-sm">Editar</a>

									</td>
								</tr>
							}
						</tbody>
					</table>

					<!-- Paginação -->
					<nav>
						<ul class="pagination">
							@for (int i = 1; i <= Model.TotalPaginas; i++)
							{
								<li class="page-item @(i == Model.PaginaAtual ? "active" : "")">
									<a href="#" class="page-link" data-page="@i">@i</a>
								</li>
							}
						</ul>
					</nav>
				</div>
			</div>
		</div> *@
	</div>
</div>

